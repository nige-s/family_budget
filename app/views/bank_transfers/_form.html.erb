<%= form_for(@bank_transfer) do |f| %>
  <% if @bank_transfer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bank_transfer.errors.count, "error") %> prohibited this bank_transfer from being saved:</h2>

      <ul>
      <% @bank_transfer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
     <div class="control-group">
     <%= f.label "Source Account owner", :class => 'control-label' %>
     <div class="controls">
    <%= f.collection_select(:src_user_id, User.all, :id, :name,:include_blank => 'Select a user') %>
     </div>
   </div>
   <div class="control-group">
    <%= f.label "Source Account", :class => 'control-label' %>
    <div class="controls">
    <%= f.collection_select(:src_account_id, Account.user_accounts(current_user.id), :id, :name,:include_blank => 'Select an Account') %>
  </div>
  </div>
     <div class="control-group">
     <%= f.label "Target Account owner", :class => 'control-label' %>
     <div class="controls">
    <%= f.collection_select(:target_user_id, User.all, :id, :name,:include_blank => 'Select a user') %>
     </div>
   </div>

   <div class="control-group">
    <%= f.label "Target Account", :class => 'control-label' %>
    <div class="controls">
    <%= f.collection_select(:target_account_id, Account.user_accounts(current_user.id), :id, :name,:include_blank => 'Select an Account') %>
  </div>
</div>



  <div class="field">
    <%= f.label :tran_date %><br>
    <%= f.date_select :tran_date %>
  </div>
  <div class="field">
    <%= f.label :amount %><br>
    <%= f.text_field :amount %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
