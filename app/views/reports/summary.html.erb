<h1>Transaction Summary</h1>
<%= link_to "Home", transactions_path %>

<%= form_for @report, :url => {:action => 'summary'} do |f| %>
<div>
    <%= f.date_select :sdate %>
    <div>
      <%= f.date_select :edate %>
  </div>
</div>
    <%= f.submit %>
<% end %>

<table>
  <thead>
    <tr>
      <th>Transaction Categories</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @cats.each do |cat| %>
      <tr>
        <% if cat.name? %>
        <td><%= cat.name %></td>
        <% else %>
        <td> Not Known </td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>
<p class='text-info' ><strong>Total spent: <%= number_to_currency(@total, unit: '£')  %></strong></p>